---
fixes:
  - |
    ``PauliEvolutionGate`` instructions are now evolved through Clifford gates in
    :func:`evolve_through_cliffords`, and the coefficient of the gate's Pauli term
    is now folded into the rotation angle during propagation. Previously, these
    gates were passed through unchanged.
